# program to find the ASCII value of the given character

# take the input from the user
character = input("Enter a character: ")

print("the ASCII value of the character " +
      character + " is:", character, ord(character))
